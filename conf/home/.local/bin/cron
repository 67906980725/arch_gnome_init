#!/usr/bin/env bash

cron() {
    [ $1 ] && sleep $1
    let i=10
    while true; do
        [ $((i % 300)) -eq 0 ] && $HOME/.local/bin/sunset_sunrise # 每300秒
        sleep 10; let i+=10
    done
}

cron 5 &